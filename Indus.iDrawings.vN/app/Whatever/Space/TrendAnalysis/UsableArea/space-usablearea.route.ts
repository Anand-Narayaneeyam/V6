import {RouterConfig} from '@angular/router';
import {SpaceUsableAreaComponent} from './space-usablearea.select';
export const SpaceUsableAreaRoutes: RouterConfig = [
    { path: 'space-usablearea', component: SpaceUsableAreaComponent }
];
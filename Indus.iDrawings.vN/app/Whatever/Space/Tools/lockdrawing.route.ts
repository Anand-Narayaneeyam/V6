import {RouterConfig} from '@angular/router';
import {LockDrawingComponent} from './lockdrawing.component';
export const lockDrawingRoutes: RouterConfig = [
    { path: 'lockdrawinglist', component: LockDrawingComponent }
];
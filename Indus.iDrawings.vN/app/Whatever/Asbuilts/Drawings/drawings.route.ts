import {RouterConfig} from '@angular/router';
import {DrawingsComponent} from './drawings.component';
export const DrawingsRoutes: RouterConfig = [
    { path: 'asbuilts-drawings', component: DrawingsComponent }
];
